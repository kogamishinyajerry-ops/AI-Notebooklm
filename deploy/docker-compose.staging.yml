version: "3.8"

services:
  notebooklm-api:
    build:
      context: ..
      dockerfile: Dockerfile
    image: comac/notebooklm:v4.0-staging
    container_name: comac_notebooklm_staging
    env_file:
      - ./staging.env
    # W-V43-11.7: keep WEB_CONCURRENCY in lock-step with `uvicorn --workers`.
    # The rate limiter consults this env var to decide between the shared
    # SQLite backend and per-process MemoryStorage; if the two drift, the
    # silent per-worker counter regression returns. Update both values
    # together when scaling workers.
    environment:
      WEB_CONCURRENCY: "1"
    ports:
      - "8000:8000"
    volumes:
      - ../data:/app/data
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    command:
      - uvicorn
      - apps.api.main:app
      - --host
      - 0.0.0.0
      - --port
      - "8000"
      - --workers
      - "1"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8000/api/v1/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
