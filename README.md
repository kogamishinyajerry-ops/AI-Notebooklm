# COMAC Intelligent NotebookLM

A highly secure, offline, enterprise-grade generic knowledge management platform inspired by Google's NotebookLM. Built strictly for Air-Gapped engineering environments.

## 🚀 Key Features

- **High-Fidelity Document Ingestion**: Parses PDFs locally using PyMuPDF, extracting granular BBox (Bounding Box) and page number metadata for every chunk of text.
- **Vanilla Dual-Pane UI**: Zero-dependency HTML5/CSS3 frontend served directly via FastAPI. Features interactive Citation-to-BBox highlighting.
- **Anti-Hallucination Gateway**: An aggressive Regex-based middleware that intercepts LLM outputs and strips any citations (file/page combinations) that were not explicitly included in the RAG retrieval context.
- **Air-Gap Ready**: Embeddings (BGE-large-zh) are frozen into the Docker image during the build process. No runtime telemetry, no cloud dependencies.

## 🏗️ Architecture

- **Backend**: FastAPI
- **Vector Store**: ChromaDB (Local Persistent)
- **Embeddings**: BAAI/bge-large-zh-v1.5 (Local Transformers)
- **Frontend**: Vanilla HTML/JS/CSS
- **LLM Engine**: OpenAI-compatible API to local vLLM instances (Qwen-2.5 / GLM-4)

## 📦 Deployment (Production)

To deploy the system in an isolated environment:

```bash
# 1. Build the image (This downloads all required models locally)
docker-compose build

# 2. Start the service
docker-compose up -d
```
The application will be available at `http://localhost:8000`.

## 🛡️ Governance Constraints

This project was developed under the strict **Antigravity Governance Framework**, enforcing:
- **C1 (Privacy Compliance)**: Absolute isolation from public endpoints.
- **C2 (Traceability)**: Every text generated by the LLM *must* be anchored to a physical coordinate (BBox) in the source document.
- **C3 (Auditability)**: All codebase iterations passed through a rigorous Requirement -> PR -> Chief Architect Audit loop via Notion.
